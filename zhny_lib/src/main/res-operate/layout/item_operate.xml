<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="android.view.View"/>
        <variable
            name="item"
            type="com.zhny.library.presenter.operate.helper.OperatorEnum" />
        <variable
            name="itemClick"
            type="com.zhny.library.presenter.operate.listener.OnOperatorItemListener" />
    </data>


    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_margin="@dimen/dp_15"
        android:layout_width="@dimen/dp_210"
        android:layout_height="@dimen/dp_210"
        android:background="#CCCCCC"
        android:onClick="@{()->itemClick.onOperateItemClick(item)}"
        >

        <ImageView
            android:layout_width="@dimen/dp_80"
            android:layout_height="@dimen/dp_80"
            android:layout_marginTop="@dimen/dp_36"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:imgSrc="@{item.imgResId}"
            tools:ignore="ContentDescription" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginBottom="@dimen/dp_37"
            android:textColor="#FF000000"
            android:textSize="@dimen/sp_28"
            app:textRes="@{item.itemNameId}"
            tools:text="数据统计"
            />


    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>